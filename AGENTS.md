# KOMBO.unit landing page

Static HTML/CSS/JavaScript. No build step or runtime dependencies.

Use `.agents/skills/frontend-design/SKILL.md` when designing and `.agents/skills/web-design-guidelines/SKILL.md` when reviewing. Brand direction: `references/design.md`.

Preserve Poppins, signal red, white and black from the supplied offer. Copy is Polish. Do not invent client testimonials, outcomes or credentials. Brand lists refer to the founders' strategy/production experience, not necessarily joint KOMBO contracts. Prices and contact come from the supplied PDF. Do not invent net/gross tax status.

Images are locally hosted, from the owner's photography portfolio. Provenance: `references/image-sources.json`. Keep original photos out of git; optimized WebP files are in assets. Keep all media relative so the site works on GitHub Pages project paths and locally.

Verify at 320/390/768/1440 px, keyboard navigation, gallery dialog, expanded offers, local links, image loading and reduced motion. Publish only the static site and assets; `.github/workflows/pages.yml` prepares this subset.
